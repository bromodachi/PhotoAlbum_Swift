(define mydel
  (lambda (l i)
    
     (cond ((null? l) l)
          ((equal? (car l) i) (cdr l))
          (else (cons (car l) (mydel (cdr l) i)))
          )
    )
  )


(define mymin
  (lambda (l))
  (cond ((null? l) l)
        ((null? (cdr l)) (car l))
        (else ((< (car l) (cadr l) ()())))))

(define sort
  (lambda (l)
    (
     (cond (null? l l)
           (else (cons 
           
           
           ))))))